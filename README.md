# Gradient Boosting Ad-Click Model

- Pipeline: data cleaning + one-hot/missing-value preprocessing + `HistGradientBoostingClassifier`
- Test accuracy (20% holdout, stratified split, random_state=42): **0.7285**
